- name : configuring the mysql server
  hosts : mysql
  become : yes

  tasks :
  - name : Install mysql
    package :
      name : mysql
      state : present

  - name : Start MySQL Service
    service :
      name : mysql
      state : started
      enabled : yes
